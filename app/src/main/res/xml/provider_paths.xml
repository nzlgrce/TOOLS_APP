<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- allow the app to share files inside getExternalFilesDir(Environment.DIRECTORY_PICTURES) -->
    <external-files-path
        name="images"
        path="Pictures" />
    <!-- fallback to internal files dir -->
    <files-path
        name="internal_images"
        path="." />
</paths>
